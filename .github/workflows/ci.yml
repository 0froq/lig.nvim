name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  ci:
    permissions:
      contents: write
      pull-requests: write
    uses: folke/github/.github/workflows/ci.yml@main
    secrets: inherit
    with:
      plugin: lig.nvim
      repo: 0froq/lig.nvim

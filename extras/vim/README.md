# Vim ColorScheme

Clone the repo somewhere.

```sh
git clone https://github.com/Fro-Q/oq.nvim ~/projects/oq.nvim
```

Add the below to your `~/.vimrc` file.

```vim
set termguicolors
set rtp+=~/projects/oqtras/vim
colorscheme oq
```

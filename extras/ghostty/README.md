To use any of these themes in Ghostty, just clone the oq repo
and set the theme to the absolute path of the theme file.

Example:

```ini
theme = "/home/Fro-Q/projects/oq.nvim/extras/ghostty/oq_night"
```
